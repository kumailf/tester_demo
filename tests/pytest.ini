# pytest.ini
[pytest]
# 设置测试用例的默认标记
markers =
    success: 标记为成功的测试用例
    failure: 标记为失败的测试用例
    error: 标记为错误的测试用例

# 设置测试用例的默认运行顺序
addopts = -v

# 设置环境变量
; env =
;     ENV_VAR_NAME=ENV_VAR_VALUE

# 设置插件
python_files = test_*.py